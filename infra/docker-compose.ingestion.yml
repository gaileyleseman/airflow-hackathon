services:
  ingestion:
    build: ingestion
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
      minio-init:
        condition: service_completed_successfully
    restart: "no"
    networks:
      - hackathon-network
